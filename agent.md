# 当前方案

本项目的完整 Agent 开发说明位于 [`AGENTS.md`](AGENTS.md)。

当前已验证稳定的字体方案是：保留 GitHub Release 的 DDS/BC3 字体纹理及其 Oodle 压缩块，只依据正确的 FT2 v14 结构修正 53 个 `m_charIdx.m_index` 路由。该方案已经过多次游戏启动验证，黑线消失且已知错字显示正确。

不要恢复全图重绘、逐字纹理重绘、旧偏移解析器或动态 Oodle 块重排路线。唯一尚未简化的字形是 `龐`；Release 字体中没有可供纯索引复用的 `庞` 字形，默认保留，以避免破坏专名或重新引入纹理风险。

